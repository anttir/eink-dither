node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json

# Stock firmware dumps read off the e-paper board. Large binaries, and the only
# copies in existence - GooDisplay does not publish them. Ignore, never delete.
device-backup/
